use teo_runtime::namespace::Namespace;
use crate::utils::file::FileUtil;

pub(crate) async fn generate_translations_index_ts(namespace: &Namespace, file_util: &FileUtil) -> teo_result::Result<()> {
    Ok(())
}