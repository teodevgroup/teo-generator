use teo_runtime::namespace::Namespace;
use crate::utils::file::FileUtil;

pub(crate) async fn generate_translations_lang_index_ts(lang: &'static str, namespace: &Namespace, file_util: &FileUtil) -> teo_result::Result<()> {
    Ok(())
}